#ifndef RECURSAO_H
#define RECURSAO_H

// Converte decimal para binário via pilha recursiva
void decToBin(int n);

// Calcula base elevado a exp (exp >= 0)
int potencia(int base, int exp);

// Soma todos os algarismos de um inteiro positivo
int somaDigitos(int n);

#endif
